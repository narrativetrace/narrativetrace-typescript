<!-- source: documentation/what-to-commit.md blob dbe573cdf91b | translated: 2026-09-07 | reviewed: - -->
# 应提交的内容

[English](../what-to-commit.md) | [Español](../es/que-commitear.md) | [Português](../pt-BR/o-que-commitar.md) | **简体中文**

NarrativeTrace 会写出描述某一次测试运行的文件。它们都不是像其他 NarrativeTrace 实现中的审批基线那样、经过审阅的手写契约——本实现尚未提供结构化/审批测试功能(已列入未来版本的计划)。下面列出的内容全部是生成产物,只有一个例外。

| 产物 | 要提交吗? | 原因 |
|---|---|---|
| `narrativetrace-output/**/*.md` | 不要 | 每次运行都会重新生成 |
| `narrativetrace-output/**/*.json` | 不要 | 与结构化 JSON 是同一条追踪——每次运行都会重新生成 |
| `narrativetrace-output/**/*.canonical.json` | 不要 | 带有模式版本号的规范化导出——每次运行都会重新生成 |
| `narrativetrace-output/diagrams/**/*.mmd` / `*.puml` | 不要 | 每次运行都会重新生成 |
| `narrativetrace-output/**/*.clarity-json` | 不要 | 按场景划分的清晰度分数——每次运行都会重新生成 |
| `narrativetrace-output/clarity-report.md` / `clarity-results.json` | 不要 | 整个测试套件的汇总结果(通过 `ClaritySuiteReporter` 生成)——是一份生成的报告,不是一个决定 |
| `glossary.json` / `glossary.md` | **要**,如果使用了术语表采集功能 | 采集完成后提交到仓库根目录;已提交的文件才是清晰度评分与词汇检查在之后每次运行时实际读取的内容——"一份文件,一套评审流程" |

`narrativetrace-output/` 目录下的一切都是输出产物。如果你还没有这样做,请把它加入 `.gitignore`:

```gitignore
narrativetrace-output/
```

## 一句话说清这条规则

如果一个文件的存在只是因为跑了一次测试,它就是输出——不要提交它。`glossary.json` 是这份列表里唯一一个需要人在落地前先审阅的文件:采集功能只是提出新增建议,而提交它们才是真正的批准(参见[清晰度指南](清晰度指南.md)与[功能指南 § 改进代码](功能指南.md#改进代码清晰度诊断))。

## 为什么这里还没有 `.approved.nt` 这一行

有些 NarrativeTrace 实现还提供一种不含具体数值的结构化产物,以及一套审批工作流——一份已提交的基线,当某个场景的*形状*发生变化时会让构建失败,需要经过审阅并被刻意地提升为新基线。本实现尚未构建这套机制。在它出现之前,你今天能拿到的最接近"经过审阅的契约"的东西,就是测试里的普通断言,再加上清晰度门禁对命名的约束。如果你现在就想要结构化 diff 的工作流,可以把 JSON 导出结果(`exportJson`/`.canonical.json`)当作你自己选用的快照测试工具的输入——对于一次与上下文无关的捕获,它是确定性的,所以快照比对能够正常工作,但 NarrativeTrace 本身目前还不会替你管理那份基线。
