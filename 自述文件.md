<!-- source: README.md blob 0e6eb7a8e007 | translated: 2026-09-12 | reviewed: - -->
# NarrativeTrace

[English](README.md) | [Español](LEAME.md) | [Português](LEIAME.md) | **简体中文**

## 从这里开始

[60 秒看到一条追踪](documentation/zh-CN/60秒.md) —— 一个普通脚本,运行一次,追踪就出现在你的终端里。

## 演示

克隆仓库并运行 `./demo.sh`。

## 示例

查看[示例](documentation/zh-CN/示例指南.md) —— NarrativeTrace 在真实应用中的样子。

## 代码即日志

零代码日志:用你的方法名和参数名作为日志。如果追踪难以阅读,需要重构的是你的代码——而不是加更多日志语句。

## 问题所在

这个方法有一半是日志噪音:

```ts
placeOrder(customerId: string, productId: string, quantity: number): OrderResult {
  console.log(`Placing order for customer ${customerId} product ${productId} quantity ${quantity}`);

  const customer = this.customers.findCustomer(customerId);
  console.log('Found customer:', customer);

  const price = this.catalog.lookupPrice(productId);
  console.log('Looked up price:', price);

  this.inventory.reserve(productId, quantity);
  console.log('Reserved inventory');

  const confirmation = this.payments.charge(customerId, price * quantity);
  console.log('Payment processed:', confirmation.transactionId);

  const result = { orderId: 'ORD-1', transactionId: confirmation.transactionId, totalCharged: price * quantity, itemCount: quantity };
  console.log('Order placed:', result);
  return result;
}
```

业务逻辑只有 5 行,日志却占了 6 行。每个开发者写日志的方式都不一样——消息不同、级别不同、包含的值也不同。结果就是不一致、冗长,还和它描述的代码纠缠在一起。

NarrativeTrace 把这一切都消除了:

```ts
placeOrder(customerId: string, productId: string, quantity: number): OrderResult {
  this.customers.findCustomer(customerId);
  const price = this.catalog.lookupPrice(productId);
  this.inventory.reserve(productId, quantity);
  const confirmation = this.payments.charge(customerId, price * quantity);
  return { orderId: 'ORD-1', transactionId: confirmation.transactionId, totalCharged: price * quantity, itemCount: quantity };
}
```

纯粹的业务逻辑。追踪由方法名、参数名和返回值自动生成——这些信息本来就已经存在了。

## 代码-日志漂移

日志语句是代码里唯一没有编译器检查、实际上也没有测试覆盖的部分,所以随着
代码变化,它们会在不知不觉中变得不再真实。改一个名字,消息还是在描述旧的
名字。加一个步骤,日志根本不会提到它。改一个单位(分 → 欧元),`total`
描述的就是另一个数字。没有什么能发现这一切:日志文本几乎从不被断言检查,
即使被检查了,断言也很脆弱,一旦挡住合并就会被删掉。过时的日志比没有日志
更糟 —— 在事故排查中,它会被当作发生过什么的证据,而它其实只是某人曾经
写下的、描述着一段已经改变的代码的一句话。

> **代码-日志漂移,从构造上就被消除。** 一行日志是对代码的一次断言,只写
> 一次,再也不会被核对。叙事追踪是从运行中派生出来的 —— 所以没有什么可以
> 漂移。

准确地说:叙述模板(`@narrated`)仍然是手写的字符串,重命名参数仍可能破坏
它的占位符 —— 这正是它在这里是例外、而不是标准路径的原因(参见
[装饰器指南](documentation/zh-CN/装饰器指南.md))。追踪中的其他一切 ——
调用、参数和结果 —— 都是派生出来的,从不是手写的,所以没有什么会过时。
而且因为追踪是结构化的,一次真正的行为变化会变成审阅者可以对比的东西,
而不是一句悄悄不再描述代码的句子。

## 你得到的是这样的结果

运行你的代码,得到这样的执行追踪:

```
OrderService.placeOrder(customerId: "C1", productId: "P1", quantity: 2)
  CustomerService.findCustomer(customerId: "C1") -> {"id": "C1", "name": "Alice", "tier": "gold"}
  ProductCatalogService.lookupPrice(productId: "P1") -> 29.99
  InventoryService.reserve(productId: "P1", quantity: 2) -> {"productId": "P1", "quantity": 2}
  PaymentService.charge(customerId: "C1", amount: 59.98) -> {"transactionId": "TX-1", "amount": 59.98}
-> {"orderId": "ORD-1", "transactionId": "TX-1", "totalCharged": 59.98, "itemCount": 2}
```

## 出错的时候

追踪让 bug 无处可藏:

```
OrderService.placeOrder(customerId: "C3", productId: "P1", quantity: 3)
  CustomerService.findCustomer(customerId: "C3") -> {"id": "C3", "name": "Charlie", "tier": "platinum"}
  ProductCatalogService.lookupPrice(productId: "P1") -> 29.99
  InventoryService.reserve(productId: "P1", quantity: 3) -> {"productId": "P1", "quantity": 3}
  PaymentService.charge(customerId: "C3", amount: 89.97) !! Error: Payment declined for customer: C3
!! Error: Payment declined for customer: C3
```

`InventoryService.reserve` 被调用了,但整条追踪里都找不到 `InventoryService.release`。bug 一目了然。

## 追踪的质量取决于你的命名

同一段 Minecraft「玩家加入世界」流程,追踪两次——一次用领域名称,一次用通用名称:

**重构后(干净的命名):**
```
WorldServer.playerJoined(playerName: "Steve")
  WorldGenerator.generateChunk(x: 0, z: 0) -> {"x": 0, "z": 0, "biome": "plains", "blockCount": 65536}
  PlayerInventory.addItem(item: {"name": "oak_log", "quantity": 4}, quantity: 4) -> true
  CraftingTable.craft(recipe: {"name": "oak_planks", "ingredients": [{"name": "oak_log", "quantity": 4}]}) -> {"name": "oak_planks", "quantity": 1}
  CreatureSpawner.spawnHostile(type: "zombie", x: 10, y: 64, z: 10) -> {"type": "zombie", "x": 10, "y": 64, "z": 10, "health": 20}
-> "Steve joined the world in plains biome"
```

**未重构(通用命名):**
```
GameManager.handle(input: "Steve")
  DataProcessor.process(a: 0, b: 0) -> {"a": 0, "b": 0, "label": "plains", "count": 65536}
  StateManager.update(item: {"name": "oak_log", "quantity": 4}, quantity: 4) -> true
  ThingFactory.create(recipe: {"name": "oak_planks", "ingredients": [{"name": "oak_log", "quantity": 4}]}) -> {"name": "oak_planks", "quantity": 1}
  EntityHandler.execute(kind: "zombie", a: 10, b: 64, c: 10) -> {"kind": "zombie", "a": 10, "b": 64, "c": 10, "value": 20}
-> "Steve joined the world in plains biome"
```

调用图一样,返回值一样,只有名字不同。如果你的代码没办法讲清楚自己的故事,它就需要重构——这也是为什么 NarrativeTrace 还会[为你的命名打分](#清晰度评分)。

## 为什么这对 AI 辅助开发很重要

每一行 `console.log(...)` 或 `logger.trace(...)`/`logger.debug(...)`/`logger.info(...)`,都是 AI 编程工具需要解析、消耗 token、并围绕它进行推理的一行代码。在典型的服务类中,日志占了 30% 到 50% 的行数。去掉这些行,你会得到:

- **每个上下文窗口容纳更多业务逻辑**——同样的 token 预算能覆盖更多你真正的代码。
- **更清晰的推理**——AI 看到的是代码做了什么,而不是它怎么记录自己做了什么。
- **只包含信号的 diff**——pull request 展示的是业务逻辑的变化,而不是逻辑和日志混在一起的变化。

这不是一个模糊的好处——它可以用 token 数量衡量。

## 清晰度评分

如果追踪*就是*代码,那么追踪的质量*就是*代码的质量。NarrativeTrace 内置了一个清晰度分析器,为你的方法名、类名和参数名打分:

```
## Clarity Report — Order Placement
Overall: 0.92 (high)

| Element     | Score | Note                    |
|-------------|-------|--------------------------|
| placeOrder  | 1.00  | Strong verb + object    |
| customerId  | 1.00  | Domain-specific noun    |
| processData | 0.30  | Generic verb + generic noun |
```

像 `processData`、`handleRequest`、`result` 这样的通用名称得分低。像 `reserveInventory`、`customerId` 这样具体到领域的名称得分高。清晰度报告会在你把它写入一次 Vitest 运行时自动生成——完整的前后对比参见[清晰度指南](documentation/zh-CN/清晰度指南.md#报告输出)。

清晰度评分目前仍属实验性功能。

## 与其他方案的比较

如果你在一个企业级技术栈里,通常已经在用:

- 集中式日志平台(Datadog、ELK、Cloud Logging)
- 分布式链路追踪后端(OpenTelemetry、Jaeger、Tempo)
- 告警和 SLO 仪表盘

NarrativeTrace 的设计目标是取代生产环境中手写的应用日志。你保留现有的 sink 和 pipeline;NarrativeTrace 成为应用事件的来源。

| 关注点 | 企业级日志技术栈 | NarrativeTrace |
|---------|--------------------------|----------------|
| 主要目标 | 可运维性、事件响应、合规 | 同样面向生产,但直接从代码结构生成 |
| 数据模型 | 手写的事件 + 字段 + span + 指标 | 带命名参数、返回值和错误的方法调用叙事 |
| 埋点方式 | 手写 `logger.*` 调用 + 遥测接线 | 自动生成的捕获,再导出到现有的企业级 sink |
| 最擅长 | 集中式检索、留存、告警 | 无需日志样板代码、高保真、低漂移的应用事件 |
| 弱点 | 事件质量取决于手写的日志语句 | 需要命名纪律才能让追踪保持清晰 |

手写的 `console.log` 是最弱的基线:分散、不一致,还和业务逻辑混在一起。NarrativeTrace 通过从代码结构派生追踪,消除了这种日志噪音。

生产环境模型:

- 保留你现有的可观测性平台和告警
- 用 NarrativeTrace 的捕获替换手写的应用日志语句
- 把 NarrativeTrace 的输出路由到你的组织已经在用的那些 sink

**OpenTelemetry 桥接:** `@narrativetrace/opentelemetry` 把叙事映射到 OTel span 上——一个实时的
`createOtelEventConsumer`,随方法执行启动/结束 span;还有一个批处理的 `TraceSpanExporter`,把捕获的
树事后转换成嵌套的 span。每个 span 都带有 `nt.trace_id`/`nt.*` 架构属性和类型化的
`narrative.param.<name>` 值,所以你现有的 Jaeger/Tempo/Datadog 追踪视图无需手工埋点就能亮起来。日志
增强器(`winston`、`pino`、`observability`)会在每一行上打上 `trace_id`、`service.*` 和 `nt.depth`,
实现同样的关联。

## NarrativeTrace 替换了什么(以及没有替换什么)

NarrativeTrace 替换的是你手写用来描述一次调用的叙述语句——不是你的日志技术栈:

```ts
logger.info(`Placing order for customer ${customerId} product ${productId}`);
```

这一行会消失;方法调用本身已经带着这些信息了。默认情况下,其他一切都不变:根本不会碰任何 logger——捕获到的内容会在进程内缓冲,再通过 `captureTrace()` 导出(Markdown/JSON/散文格式的文件),或者在浏览器里用 `renderToConsole` 事后打印出来。

接入 `@narrativetrace/pino` 或 `@narrativetrace/winston`,NarrativeTrace 就会变成一个实时的发送方:`createPinoEventConsumer`/`createWinstonEventConsumer` 会直接调用你自己的 `Logger` 实例(`logger.trace()`/`logger.warn()`,可按事件配置),所以你已经配置好的每一个 transport、formatter 和投递目的地都会照常工作、不受影响——NarrativeTrace 只是你的 logger 多了一个调用方,不是取代它。你出于自己的目的继续手写的日志——一条审计记录、一个业务指标,任何不只是叙述控制流的内容——都会跑在同一个 logger 上,和 NarrativeTrace 自己的日志行交错在一起。只想要关联信息、不想生成任何日志行?`@narrativetrace/observability` 的 `createLogEnricher` 会把 `trace_id`/`nt.*` 字段盖到你仍然手写的 logger 调用上;它自己从不发出任何日志。

## 加到一个测试里

在你已有的测试套件里得到一条追踪,最短的路径是 Vitest fixture——要说最短中的最短,一个不需要
测试框架的简单脚本,见[60 秒看到一条追踪](documentation/zh-CN/60秒.md)。Node 20+(CI 运行
22),如果要用下面的装饰器,还需要 TypeScript 5.0+。

```bash
pnpm add -D @narrativetrace/vitest @narrativetrace/proxy vitest
```

`vitest` 是 `@narrativetrace/vitest` 唯一的 peerDependency(对等依赖）；它的另外四个 NarrativeTrace 依赖包(core-node、clarity、diagrams、glossary)会随它一起自动安装——它们与它同步发布，从不单独设定版本号。之所以显式写出 `@narrativetrace/proxy`，是因为下面的测试直接从它导入 `traceObject`:pnpm 只会暴露一个包自身的依赖，不会暴露依赖的依赖,所以任何你自己 `import` 的东西都必须是你自己项目的直接依赖(npm 更扁平的 `node_modules` 不区分这一点,但这里演示的 pnpm 会区分)。*(since 0.1.3, unreleased)*

这些是已发布的包——`npm view @narrativetrace/core version` 能看到当前版本(写这段文字的时候是
0.1.1;如果你需要一个尚未发布的改动,见[从源码构建](#从源码构建))。

```ts
// order-service.test.ts
import { traceObject } from "@narrativetrace/proxy";
import { createNarrativeTest } from "@narrativetrace/vitest";
import { OrderService } from "./order-service.js";

const test = createNarrativeTest();

test("customer places order", ({ narrativeContext }) => {
  const service = traceObject(new OrderService(), narrativeContext, {
    placeOrder: ["customerId", "productId", "quantity"],
  });

  service.placeOrder("C1", "P1", 2);
});
```

运行 `npx vitest run`,打开 `narrativetrace-output/order-service/customer_places_order.md`——测试名变成了场景名,不需要任何接口或构建工具插件(`traceObject` 直接包装具体对象)。

想看看重命名一个方法会怎样让清晰度分数下降,或者用 `@notTraced` 隐去一个值?
[清晰度指南](documentation/zh-CN/清晰度指南.md)和[隐私与脱敏](documentation/zh-CN/隐私与脱敏.md)
会带你把两者都走一遍,全部都是真实运行出来的输出。想先看最短的路径?→
[60 秒看到一条追踪](documentation/zh-CN/60秒.md)。

## 选择你的集成方式

大多数人从测试开始。接下来该往哪走:

| 你想要 | 从这里开始 |
|---|---|
| 测试里的追踪,接线最少 | `@narrativetrace/vitest`(`createNarrativeTest`) |
| 精确选择要包装什么,纯 TypeScript/JavaScript | 直接用 `@narrativetrace/proxy`(`traceObject`) |
| Express 应用里按请求追踪 | `@narrativetrace/express` |
| Hono(edge/serverless)里按请求追踪 | `@narrativetrace/hono` |
| 零应用代码的 Nest provider 追踪 | `@narrativetrace/nestjs`(`AutoProxyModule`) |
| Angular 服务/DI 追踪 + HTTP 关联 | `@narrativetrace/angular` |
| React 组件/服务追踪 | `@narrativetrace/react`(导航追踪再加 `@narrativetrace/react-router`) |
| 带打包工具的浏览器页面 | `@narrativetrace/core-web` + `@narrativetrace/browser` |
| 不带打包工具的浏览器页面,经典 `<script>` | `@narrativetrace/standalone` |
| Node 上的跨请求/异步可见性 | `AsyncNarrativeContext`(基于 `AsyncLocalStorage`) |
| 生产日志流中的追踪 | `@narrativetrace/winston` 或 `@narrativetrace/pino` |
| OpenTelemetry span | `@narrativetrace/opentelemetry` |

这里没有零代码、「包装一个你没写过的应用」这条路——没有 Java agent 那样的等价物。`Proxy` 和装饰器都需要一个调用点,或者一个你能加注解的类;`require`/ESM 加载器 hook 被有意舍弃了,因为它在不同 Node 版本之间很脆弱,而且会被打包工具和浏览器完全绕过。完整的决策图、每条路径的注意事项,以及平台上限背后的理由,见[选择集成方式](documentation/zh-CN/选择集成方式.md)。

## 包

全部 21 个包:

| 包 | 什么时候需要它 |
|---------|---------------------|
| `@narrativetrace/core` | 总是需要。运行时零依赖;与平台无关。 |
| `@narrativetrace/core-node` | Node 运行时:`AsyncNarrativeContext`(AsyncLocalStorage)、`NARRATIVETRACE_*` 环境变量配置、关闭时自动刷新。 |
| `@narrativetrace/core-web` | 为平台无关核心提供的浏览器运行时接缝。 |
| `@narrativetrace/proxy` | 使用 ES Proxy 追踪(最常见)。 |
| `@narrativetrace/vitest` | Vitest 测试中的自动追踪 + 按测试的清晰度/失败报告。 |
| `@narrativetrace/diagrams` | 生成 Mermaid/PlantUML 时序图。 |
| `@narrativetrace/clarity` | 分析方法/参数命名质量;`clarity-results.json` gate。 |
| `@narrativetrace/glossary` | 从追踪中收集并渲染领域词汇表(通用语言);为 clarity 的项目词汇和翻译后的追踪视图提供数据。 |
| `@narrativetrace/browser` | 浏览器控制台渲染和网络导出。 |
| `@narrativetrace/standalone` | 面向不用打包工具的纯 JavaScript 页面的单文件打包(经典全局 `<script>` 或 ES 模块)。 |
| `@narrativetrace/angular` | Angular 集成:`provideNarrativeTrace()`、拦截器、DI 追踪。 |
| `@narrativetrace/react` | 用于捕获组件 + 服务追踪的 React hooks/provider。 |
| `@narrativetrace/react-router` | React Router 导航捕获。 |
| `@narrativetrace/express` | Express 中间件:按请求上下文、fail-safe 提取器、`onRequestComplete`。 |
| `@narrativetrace/hono` | Hono 中间件(edge/serverless),与 `finally` 完成语义对等。 |
| `@narrativetrace/nestjs` | NestJS 的 `AutoProxyModule.forRoot({ pipeline, consumers, onRequestComplete })`。 |
| `@narrativetrace/observability` | 日志作用域增强器(`code.*`、`trace_id`、`service.*`、`nt.depth`)+ 请求中间件。 |
| `@narrativetrace/opentelemetry` | OTel 桥接:实时 `createOtelEventConsumer` + 批处理 `TraceSpanExporter`。 |
| `@narrativetrace/winston` | 带类型化字段和可配置逐事件级别的 Winston consumer。 |
| `@narrativetrace/pino` | 带类型化字段和可配置逐事件级别的 Pino consumer。 |

**典型的起点:** `core-node`/`core-web` + `proxy` + `vitest`。

## 并发支持

并行的工作依旧保持可读。`ForkJoinGroup` 会把父级的追踪身份(traceId、请求/用户上下文)传播到每个
被 fork 出去的任务中,并记录每个成员的耗时;`FireAndForgetGroup` 启动的后台工作,依然会出现在追踪里。

```ts
import { ForkJoinGroup, FireAndForgetGroup } from '@narrativetrace/core';

// Fork/join —— 在一个共享的组下并行执行价格 + 库存检查。
const [price, stock] = await ForkJoinGroup.all(context, [
  (ctx) => traceObject(pricingService, ctx).quote('P1'),
  (ctx) => traceObject(inventoryService, ctx).check('P1'),
]);

// Fire-and-forget —— 一条不能阻塞响应的通知。
const bg = FireAndForgetGroup.create(context);
bg.launch((ctx) => traceObject(notificationService, ctx).sendReceipt('C1'));
```

Markdown 渲染器会展示并发结构,以及时间到底花在了哪里:

```
- ⑂ fork [2 tasks]
  - ↦ `InventoryService.check("P1")` → `true` — 40ms
  - ↦ `PricingService.quote("P1")` → `"12.50"` — 110ms
- ⑃ join — 110ms (waited 70ms for PricingService after InventoryService)
```

通过上下文快照(而不是一个组)传播的工作,会以另一种方式并入发起它的那条追踪:它从发布调用的那一刻起
就被上报,而不是等到它的作用域关闭才上报,它的第一个 span 会被标记为 `concurrency.kind === "async"`。
那些自己发布子节点的 helper,可以用 `snapshot.activateWithoutAdoption(...)` 主动退出。参见
[框架集成指南](documentation/zh-CN/框架集成指南.md#contextsnapshot跨边界传播)。

在共享的浏览器 `SyncNarrativeContext` 上,没有 `await` 的重叠调用是不安全的——每个任务都要用显式的
fork/fire-and-forget(参见[框架集成指南](documentation/zh-CN/框架集成指南.md))。

## 隐私与安全

这个库运行在你的进程内部,会写出你团队将会共享的文件。这意味着什么,一屏说清楚:

| 保证 | 如何做到 |
|---|---|
| **每个已发布的集成都遵守脱敏(redaction)规则** | `traceObject()` 是每个集成(`express`、`hono`、`nestjs`、`angular`、`react`、`vitest` 等等)构建其上的唯一捕获路径,它们都没有暴露任何能到达 `RedactionPolicy.DISABLED` 的方式。`@notTraced`/`static notTraced` 始终优先——即便是在一个应用明确关闭脱敏的渲染器之下。 |
| **脱敏在嵌套和模板中依然有效** | 一个被脱敏的成员,不管是在数组、`Set`、`Map`、普通对象、多层嵌套还是自引用环里,依然保持脱敏;一个 `{param.property}` 叙事模板,只要指向一个被脱敏的成员,解析结果就是 `[REDACTED]`,永远不会是真实的值。 |
| **追踪失败不能让你的应用失败** | 捕获在设计上就是尽力而为的——一个抛异常的自定义 `toString()`、一个在模板里被点名的抛异常 getter,或者一个已满的缓冲区,都会降级为一次未被追踪的调用,永远不会阻塞或让业务方法失败。 |
| **资源使用是有边界的** | 带缓冲的分析路径是一个固定大小的环形结构(测试中默认 8192 个事件,长期运行的进程中默认 65536 个),它会丢弃事件而不是阻塞——而且会说明这一点:丢失了事件的捕获会在自己的页脚里打印丢失的数量,以及该如何提高上限。 |

两个诚实的限制。第一,值能逃脱脱敏的唯一方式,是应用代码直接调用底层渲染器,并显式传入
`RedactionPolicy.DISABLED`——这是你自己源码里一个刻意、可审查的行为,即便如此,
`@notTraced`/`static notTraced` 注解依然会继续脱敏。第二,捕获在渲染时会调用你代码里一小组固定的部分——
一个自定义 `toString()`、一个 `@narrativeSummary` 方法,以及在 `@narrated`/`@onError` 模板里被点名的
属性路径——所以请让它们保持纯净,就像你会为调试器做的那样。这里也没有零代码、「包装一个你没写过的应用」
这条路,而且这个实现 没有发布一个不含值的结构化产物(NarrativeTrace 的其他 port 有这样的产物)——
下面两页给出了两者精确的、逐行的说明。

→ [隐私与脱敏](documentation/zh-CN/隐私与脱敏.md),看经过代码验证的逐行契约;
[应提交的内容](documentation/zh-CN/应提交的内容.md),看哪些生成出来的文件应该留在版本控制之外。
当另一个库也包装了同样的方法时(一个 DI 容器、另一个 `Proxy`、一个契约库),NarrativeTrace 只会叙述
业务边界的跨越,而哪一个包装器处在「外层」,永远不会改变最终传到调用方的结果或异常——完整的共存契约见
下面的常见问题。

## 性能

追踪是要做工作的,而工作总有代价——我们不会声称「零开销」。这个 proxy 通过 ES `Proxy` 拦截调用、
捕获参数、把值渲染成字符串,并构建追踪树。当追踪被关闭时,包装在构造上就是零工作:用
`NOOP_CONTEXT` 包装会返回**原始对象本身**(没有代理,每次调用没有任何成本);而处于
`level: 'off'` 的活动上下文会保留代理(级别可以在运行时切换),但每次调用不做任何捕获工作——
只有一次包装器缓存查询和一次 `isActive` 检查,没有内存分配,没有渲染。

实测(2026-09-07,Node 22,本仓库的 Linux 开发容器,`packages/benchmarks` 的
`proxy.bench.ts`):一个两参数的简单方法裸跑约 9.8M ops/s;同一调用经过 `level: 'off'`
的包装器约 4.3M ops/s——每次调用增加约 0.1 µs;`NOOP_CONTEXT` 的包装与裸对象无法区分,
因为它*就是*裸对象。追踪完全开启时(`detail`:渲染参数 + 返回值),同一简单调用约 105K
ops/s(每次调用约 10 µs)——这是真正把故事渲染出来的成本。

`packages/benchmarks/` 目录包含了针对上下文进入/退出、proxy 开销、值渲染,以及不同树大小下
Markdown/JSON 渲染的 Vitest 基准测试,并把基线保存在 `reports/benchmarks/` 下,让回归在多次提交之间
保持可见。运行 `pnpm run bench`(或者 `pnpm run bench:save` 来和保存的基线做对比),就能在你自己的
硬件上复现上面的数字——我们把这些当作你应该自己复现的测量结果,而不是标题党式的数字,因为容器和机器的
负载会让它们在每次运行之间发生变化。

对于极度高频的循环,使用 `level: 'off'`,或者把追踪的范围收窄到真正重要的边界。

## 哪些是免费的,哪些是 Pro

**免费**版是这个仓库里的一切——以源码可见的方式发布,基于 BSL 1.1,在生产环境中免费使用,并在每次
发布之后的四年转为 Apache 2.0:整个运行时、每种格式的按测试追踪(Markdown、JSON、规范化 JSON、
Mermaid、PlantUML)、清晰度评分和领域词汇表,以及上面表格里的每一种集成。

**Pro** 版是跨多次运行的智能能力:事件流聚合(`@narrativetrace/pro-aggregate`,热点、错误路径/比率、
方法/错误频率)和一个把 Claude Code / Cursor 直接连接到你的追踪上的 MCP 服务器正在开发中;流程摘要、
迁移差异、依赖关系图,以及一套审计与合规工具都已在规划中。并不是所有这些今天都已经发布——
[功能指南](documentation/zh-CN/功能指南.md)是权威的状态表:它把每项功能标记为免费、Pro、开发中或
已规划,并为每一行已发布的功能引用背后的代码。

## 文档

从这里开始:

- [60 秒看到一条追踪](documentation/zh-CN/60秒.md)——一个简单脚本包装一个服务,运行一次,追踪出现在你的终端里
- [安装指南](documentation/zh-CN/安装指南.md)——依赖、每条集成路径、追踪输出的配置
- [选择集成方式](documentation/zh-CN/选择集成方式.md)——你需要哪个包,以决策图的形式呈现
- [配置指南](documentation/zh-CN/配置指南.md)——追踪级别、Vitest 配置、渲染选项
- [装饰器指南](documentation/zh-CN/装饰器指南.md)——`@traced`、`@narrated`、`@onError`、`@notTraced`

深入了解:

- [隐私与脱敏](documentation/zh-CN/隐私与脱敏.md)——经过代码验证的逐行脱敏契约
- [应提交的内容](documentation/zh-CN/应提交的内容.md)——哪些生成的文件属于运行输出,哪些(如果有的话)是需要审查的基线
- [故障排查](documentation/zh-CN/故障排查.md)——症状 → 原因 → 解决办法,针对大家实际会遇到的失败模式
- [清晰度指南](documentation/zh-CN/清晰度指南.md)——评分模型、NLP 组件、静态扫描器
- [框架集成指南](documentation/zh-CN/框架集成指南.md)——Express、Hono、浏览器、AsyncLocalStorage
- [示例指南](documentation/zh-CN/示例指南.md)——`pnpm demo` 启动器和可运行的示例:ecommerce、clarity、Minecraft、纯 JavaScript、Express、Hono、分布式(Docker + Jaeger)、浏览器
- [功能指南](documentation/zh-CN/功能指南.md)——这个实现 已发布内容的权威目录,附带 tier 和状态

## 从源码构建

```bash
pnpm install                                      # 安装依赖
pnpm run check                                    # lint + 指标 + 覆盖率 + 变异测试
pnpm run build                                    # 构建所有包
pnpm run test                                     # 运行所有测试
```

## 验证一切

`pnpm run check` 是每次提交都会跑的检查;`pnpm run verify:all` 会一次性运行本仓库拥有的*每一项*验证——无论是轻量的还是重量级的:单元测试、覆盖率、变异测试、属性测试与模糊测试、基准测试、架构规则、两档压力测试、规范模式一致性检查,以及 secrets/SAST/SCA 扫描器——并写出一份带日期的报告。

```bash
pnpm run verify:all                               # 设计上就是长时间运行——见下文
```

它**设计上就是长时间运行**的:在整个工作区上跑变异测试是最慢的一项(在配置一般的容器上要几十分钟)。任何一项检查失败都不会中止整个运行——每一项都会跑到,命令只在最后才以非零状态码退出。结果可以在 `reports/verification/<date>.json` 里读到(每个类别一行:工具、状态、指标、耗时),以及直接由它渲染出的 `reports/verification/<date>.md` 表格。

## 常见问题

### 这会带来多大的开销(overhead)?高并发下会怎样?

我们不会说"零开销"——上面的[性能](#性能)一节给出了这个回答所依据的、带日期的数据(2026-09-07,Node 22,本仓库的容器环境):在追踪开启但处于 `level: 'off'` 时,一次调用比不追踪的调用大约多花 **0.1 微秒**;在完整细节级别(渲染参数和返回值)下,大约花费 **10 微秒**。NarrativeTrace 本身增加的只是捕获——拦截调用、读取参数、构建追踪树。捕获之后的一切(写盘、collector、网络跳转)和你现有日志系统已经在付出的成本完全一样;NarrativeTrace 不会再加一个新的写入目的地。对于正在替换手写 `console.log`/`logger.debug` 调用的团队来说,目的地这一侧基本是打平的:每个方法原本的 N 条日志写入,变成一次追踪写入,而那些日志语句本身也不再需要被编写、审查、和代码保持同步。

在并发场景下,默认 `DualPathPipeline` 的两条路径有不同的保证。如果你接入了一个同步 listener(比如把事件接到 winston/pino 上),它会在调用方自己的执行流里同步运行,所以它和你现有的日志调用一样可靠——开销也完全一样。真正给 `captureTrace()` 供数据的缓冲分析路径,是一个固定大小的环形缓冲区(默认 65,536 个事件,可按上下文调整大小——见[配置指南 § 8](documentation/zh-CN/配置指南.md#8-事件管道缓冲bufferedeventconsumer)),由定时器负责清空。它绝不会阻塞调用方:达到容量上限时,会覆盖最早的未清空事件,并把它**计入** `overflowCount()`,而不是悄悄丢弃——所以持续的过载是可见的,不用去猜。

**诚实地说明这个缺口:** 目前这个实现、乃至所有 NarrativeTrace 实现,都还没有采样(sampling)——每次被追踪的调用都会按配置的级别完整捕获。按百分比或按速率限流的采样器在路线图上,但还没发布。如果你现在就需要限制捕获量,可以把追踪范围收窄到真正重要的边界,或者把热路径降到 `level: 'off'`/`'errors'`。

### 我怎么知道带有 PII 或凭据的参数不会泄漏到追踪里?

四层独立的保护,而不是一句笼统的承诺——完整的、逐行对照代码验证过的合同见[隐私与脱敏](documentation/zh-CN/隐私与脱敏.md):

1. **参数上的 `@notTraced(i)` / 类上的 `static notTraced = [...]`** ——由你显式控制的脱敏,按索引或按字段名。这一层始终优先,哪怕你的技术栈里有别的代码用关闭脱敏的选项调用了底层渲染器。
2. **始终开启、覆盖多语言的按名称拒绝列表** ——每条捕获路径都会用 `password`、`secret`、`token`、`ssn`、`cvv`、`apikey`、`cardNumber`、`passphrase`、`bearer`、`taxId` 等模式,加上西班牙语(`contraseña`、`tarjeta`、`dni`、`rut`……)、葡萄牙语(`senha`、`cpf`、`cnpj`)、德语(`passwort`、`kennwort`)、中文(`密码`、`身份证`)等对应词,去匹配字段名和参数名。这是默认开启的,不需要手动启用;最容易误伤的那批模式只在标识符的词边界上匹配——`panelId`、`circuitBreaker` 不会被 `pan`/`cuit` 命中。
3. **不依赖字段名的值形状匹配** ——一个 JWT 形状的字符串、一个通过 Luhn 校验的卡号、一个 `Set-Cookie` 形状的值,或者一个国民身份校验位或结构规则(智利 RUT、巴西 CPF/CNPJ、西班牙 DNI/NIE、法国 NIR、中国居民身份证,或者一个带连字符的美国社会安全号码——唯一没有校验位的例外,用 SSA 自己从未签发过的地区/组别/序列号段来代替),即使出现在 `data`、`value` 这类无害的名字下面,也会被脱敏。
4. **这个实现目前还没有不携带值的结构化模式。** 其他一些 NarrativeTrace 实现提供一种 `.nt` 风格的产物,携带调用图和结构信息但完全不含运行时的值——这是"没有任何值可能离开进程"这类场景(比如把追踪交给外部 AI 工具)下的范畴性保证。TypeScript 还没有构建这个([原因](documentation/zh-CN/应提交的内容.md#为什么这里还没有-approvednt-这一行));在它出现之前,这个实现生成的每一份产物都携带真实的值,由上面三层保护,而不是从结构上就不存在风险。

要把边界说清楚:按名称和按形状的匹配都是启发式的、可扩展的——发现新的缺口就会加新模式,但也总可能漏掉一个还没被命名的情况。它不是不携带值模式那种范畴性保证。如果你的威胁模型要求"任何值都不可能离开进程",这个实现今天还满足不了这个要求。

### 追踪 ID 能和跨服务的标准关联 ID(correlation ID)对上吗,还是只能在本地用?

可以,机制和 OpenTelemetry 自己用的一样:W3C 的 [`traceparent`](https://www.w3.org/TR/trace-context/)。`parseTraceparent()` 读取入站请求头并延续上游的追踪;`formatTraceparent()`(core 包)以及浏览器/Angular 集成里的 `tracedFetch()`/`traceInterceptor` 会把它盖在出站请求上。NarrativeTrace 生成的追踪 ID 从一开始就是 W3C 格式(32 位小写十六进制字符),所以它和你的 OTel collector 或关联 ID 中间件已经理解的是同一个 ID——不需要另外做协调。[`opentelemetry`](documentation/zh-CN/功能指南.md) 集成还会把 NarrativeTrace 的 span 导出成带类型化 `narrative.param.*` 属性的形式,[示例指南](documentation/examples-guide.md)里的分布式追踪示例演示了多个服务端到端共享同一个 `traceId`。

留在本地的部分:叙事树本身——嵌套的方法调用、参数、叙述文本——是按进程捕获的,不会发送给其他服务,发送出去的只有追踪 ID。下游服务会生成自己的叙事树,和同一个 ID 关联,而不是一棵合并起来的跨服务树。

### 值的序列化是怎么工作的?

NarrativeTrace 使用**即时(eager)序列化**——参数值和返回值在捕获的那一刻就被渲染成字符串,然后才被存进追踪里。这是一个刻意的设计选择:

- **正确性:** 对象是按调用那一刻的样子被捕获的。如果一个可变对象在被追踪的调用返回之后又被修改,追踪展示的依然是原始值。
- **不持有对象:** 追踪只保存字符串,不保存对你的领域对象的引用。你的对象可以被垃圾回收,不受任何阻碍。
- **安全渲染:** 内置的 `renderValue()` 能处理:null、undefined、字符串、数字、布尔值、数组、普通对象、BigInt、symbol,以及循环引用。较大的值会被截断(`maxStringLength`、`maxArrayItems`、`maxObjectKeys`)。

### 追踪会在我的代码里触发副作用吗?

只会在一小组有文档记录的地方触发。内省只会枚举自身可枚举的属性(`Object.keys`)——定义在类上的 getter 位于原型上,永远不会被执行。NarrativeTrace *真正会*调用的成员是:一个自定义的 `toString()`、一个 `@narrativeSummary` 方法,以及在 `@narrated`/`@onError` 模板里被点名的属性路径。请让它们保持纯净,就像你会为调试器或序列化器做的那样——或者把这个字段列进 `static notTraced`,这样它的值就完全不会被读取。每一次调用都是有边界、且与异常隔离的(一个抛异常的 getter 永远不会让你的业务调用失败),thenable 永远不会被 `await`,而当追踪关闭时,不会发生任何渲染。参见[装饰器指南](documentation/zh-CN/装饰器指南.md)里的纯净性契约。

### 它会追踪私有方法吗?

不会——ES 的 Proxy 只追踪对象上的公开方法。但私有方法会通过它们调用的服务而变得可见:

```ts
private fulfillOrder(order: Order) {
  if (order.isDigital) {
    this.deliveryService.sendDownloadLink(order.customerId, order.productId);
  } else {
    this.warehouseService.shipPhysical(order.customerId, order.shippingAddress);
  }
  this.notificationService.confirmOrder(order.customerId, order.orderId);
}
```

追踪会显示走了哪个分支:

```
OrderService.placeOrder(customerId: "C1", productId: "SKU-EBOOK")
  DeliveryService.sendDownloadLink(customerId: "C1", productId: "SKU-EBOOK") -> "https://..."
  NotificationService.confirmOrder(customerId: "C1", orderId: "ORD-001") -> true
```

不需要追踪那个 `if`——`sendDownloadLink` 的出现和 `shipPhysical` 的缺席,已经把故事讲清楚了。ES 的 `#private` 字段没办法被 Proxy 拦截(这是 JavaScript 语言本身的限制),但架构上的好处是一样的。

### 为什么自调用不会嵌套?

被追踪的方法执行时,`this` 绑定的是原始对象而不是代理(方法包装器内部的 `Reflect.apply(fn, target, args)`)。因此,方法调用同一对象上的兄弟方法(`this.validate(order)`)时,调用的是原始方法——调用会正确执行,但不会被捕获,所以自调用永远不会以嵌套 span 的形式出现。这是刻意的设计取舍,不是缺陷:绑定原始对象让代理免疫 Proxy 的几大经典陷阱——`#private` 字段(经代理接收者访问会抛出)、带内部槽的内置对象(`Map`、`Date`)和箭头函数字段。

嵌套来自包装协作者,这也是唯一的结构规则:**拆分为协作者服务,在每个服务被构造的地方将其包装。**在组合根处把 `OrderService`、`InventoryService`、`PaymentService` 各包装一次,就能得到完整的嵌套叙事——这也正是无论追踪与否都最易读的代码形态。

### NarrativeTrace 如何与其他包装方法的库(AOP、代理、契约库)交互?

NarrativeTrace 只叙述业务边界的跨越,不叙述底层机制。它自身的挂载机制是可选的,而且设计得很窄:
`traceObject()` 一次只包装一个对象,用一个只实现了 `get` trap 的 ES `Proxy`,所以其他所有操作——属性
枚举、`instanceof`、原型访问——都会原样传递给包装同一个对象的其他任何东西。一份内置的排除列表,
避免了强制类型转换/内省相关的钩子(`toString`、`valueOf`、`Symbol.toPrimitive`)被当作业务调用叙述
出来,这份排除列表的设计意图是随着发现新的漏洞不断增长,而不是缩减。

当另一个库也包装了同样的方法时——一个契约库、一个 AOP proxy、一个 DI 容器拦截器——哪一个最终处在
「外层」,只会改变追踪帧的表面嵌套方式,永远不会改变哪些事实进入叙事,也不会改变最终的业务结果:
一个抛出的异常或一个返回的值,总会原封不动地穿过每一层。违规捕获在原则上被设计成与顺序无关——一个
事实应该作为一个由其自身来源发出的事件进入叙事,而不是靠观察它如何穿过某个包装器来推断——不过
NarrativeTrace 目前还没有公开一个 API,让第三方库把这样一个事实注入一条正在进行中的追踪;这被记录为
未来的工作,而不是今天就承诺的功能。

现在可用的手段,是用 `@notTraced`/`static notTraced` 作用在你能控制的类上,以及干脆不在你不想被叙述
的表面上调用 `traceObject()`——目前还没有一份仓库级别默认的排除列表,能按名称模式排除另一个库生成的
类。

## 许可

NarrativeTrace 的 API 和输出格式是开放标准(Apache 2.0)。它的运行时是免费且源码可见的
(BSL 1.1,在每次发布之后的四年转为 Apache 2.0)。Pro 是商业化的。

这对这个仓库里的各个包意味着什么:

| 部分 | 许可 |
|---|---|
| 运行时——从这个仓库发布的每一个 `@narrativetrace/*` 包 | [BSL 1.1](LICENSE)(SPDX `BUSL-1.1`),在每次发布之后的四年转为 Apache 2.0 |
| 注解/装饰器 API、输出格式规范和清晰度评分标准 | [Apache 2.0](LICENSE-APACHE)(API 拆分尚未完成——见下文) |
| 文档正文 | [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/) |

Additional Use Grant 允许出于任何目的的生产使用,包括用在你提供给自己客户的产品和服务里;唯一的
例外,是把 NarrativeTrace 本身——或者一个价值主要来自它的产品或服务——作为日志、追踪或代码叙事类
的产品或服务提供给第三方。确切条款见 [LICENSE](LICENSE),或者就其他安排联系 <hello@narrativetrace.ai>。

<!-- legal:trademark:begin -->
NarrativeTrace 是 Empower Agile 的商标。许可并不授予任何商标权利。
<!-- legal:trademark:end -->

### 用大白话说的许可

这个仓库里的一切目前都在 Business Source License 1.1 之下发布——采用 Apache 许可的部分(注解/装饰器
API、输出格式规范、清晰度评分标准)目前还没有拆分成独立的包。

<!-- legal:plain-words:begin -->
**免费运行。** 运行时依据 Business Source License 1.1 源码可得:你可以阅读它、审计它、修改它,
并可免费用于生产环境——包括用在你提供给自己客户的产品与服务中。

**唯一的排除项。** 你不得将 NarrativeTrace 本身——或价值实质上源自它的产品或服务——作为日志、
追踪或代码叙事产品或服务提供给第三方。

**它会在某个日期开放。** 每个发布版本都会在发布四年后转为 Apache 2.0;
确切日期印在该版本的 LICENSE 中。

*这份摘要只是一种礼貌说明,不是许可证本身。LICENSE 文件才是唯一具有约束力的文本;
若二者不一致,以 LICENSE 为准。*
<!-- legal:plain-words:end -->
