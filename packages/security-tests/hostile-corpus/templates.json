{
  "$comment": "@Narrated / @OnError template strings. `values` names the fixture graph the template is resolved against (see README.md). `expect` pins the one thing that must be true of the result.",
  "cases": [
    { "id": "plain", "description": "the well-formed baseline", "template": "charging {card.number}", "values": "card" },
    { "id": "empty", "description": "an empty template", "template": "", "values": "card" },
    { "id": "no-placeholder", "description": "prose with no placeholder at all", "template": "nothing to resolve", "values": "card" },
    { "id": "redacted-component", "description": "names a @NotTraced record component directly", "template": "charging {card.cvv}", "values": "card", "expect": "redacted" },
    { "id": "redacted-by-name", "description": "names a deny-listed property no annotation marks", "template": "login {user.password}", "values": "user", "expect": "redacted" },
    { "id": "redacted-mid-path", "description": "a redacted segment in the middle of a longer path", "template": "charging {order.card.cvv}", "values": "order", "expect": "redacted" },
    { "id": "redacted-deep-path", "description": "the redacted segment is the last of a five-segment path", "template": "{a.b.c.d.secret}", "values": "deep", "expect": "redacted" },
    { "id": "redacted-repeated", "description": "the same redacted path twice in one template", "template": "{card.cvv} and {card.cvv}", "values": "card", "expect": "redacted" },
    { "id": "redacted-adjacent", "description": "a redacted path directly against a safe one, no separator", "template": "{card.number}{card.cvv}", "values": "card", "expect": "redacted" },
    { "id": "brace-noise-around-a-redacted-path", "description": "extra braces make the whole thing literal - nothing resolves, so nothing can leak", "template": "{{card.cvv}}", "values": "card" },
    { "id": "unknown-root", "description": "a mistyped root: resolves to nothing, so nothing can leak", "template": "charging {custmer.cvv}", "values": "card" },
    { "id": "unknown-member", "description": "a path naming no member at all", "template": "charging {card.nothingHere}", "values": "card" },
    { "id": "unterminated-open", "description": "an opening brace with no close", "template": "charging {card.number", "values": "card" },
    { "id": "unterminated-close", "description": "a closing brace with no open", "template": "charging card.number}", "values": "card" },
    { "id": "empty-placeholder", "description": "a placeholder with no path inside it", "template": "charging {}", "values": "card" },
    { "id": "blank-placeholder", "description": "a placeholder holding only whitespace", "template": "charging {   }", "values": "card" },
    { "id": "dot-only", "description": "a path that is a single separator", "template": "{.}", "values": "card" },
    { "id": "leading-dot", "description": "an empty first segment", "template": "{.cvv}", "values": "card" },
    { "id": "trailing-dot", "description": "an empty last segment", "template": "{card.}", "values": "card" },
    { "id": "double-dot", "description": "an empty middle segment: names no member, so the walk stops and the placeholder stays literal", "template": "{card..cvv}", "values": "card" },
    { "id": "nested-braces", "description": "a placeholder inside a placeholder", "template": "{outer{card.cvv}}", "values": "card" },
    { "id": "nested-deep", "description": "twenty nested opening braces", "repeat": { "unit": "{", "count": 20 }, "suffix": "card.cvv", "values": "card" },
    { "id": "many-placeholders", "description": "a thousand placeholders in one template", "repeat": { "unit": "{card.number}", "count": 1000 }, "values": "card" },
    { "id": "many-open-braces", "description": "ten thousand unterminated placeholders", "repeat": { "unit": "{", "count": 10000 }, "values": "card" },
    { "id": "path-depth-50", "description": "a fifty-segment path - deeper than any real object graph", "prefix": "{a", "repeat": { "unit": ".b", "count": 50 }, "suffix": "}", "values": "deep" },
    { "id": "unicode-identifier", "description": "a path whose segments are valid Java identifiers outside ASCII", "template": "{\u0063af\u00e9.na\u00efve}", "values": "unicode" },
    { "id": "unicode-identifier-redacted", "description": "the same, naming a redacted member", "template": "{\u0063af\u00e9.secret}", "values": "unicode", "expect": "redacted" },
    { "id": "zero-width-in-path", "description": "an invisible character splitting an otherwise valid path", "template": "{card.cv\u200bv}", "values": "card" },
    { "id": "rtl-in-path", "description": "a bidi override inside a path", "template": "{card.\u202ecvv}", "values": "card" },
    { "id": "newline-in-path", "description": "a line break inside a placeholder", "template": "{card.\ncvv}", "values": "card" },
    { "id": "nul-in-path", "description": "a NUL inside a placeholder", "template": "{card.\u0000cvv}", "values": "card" },
    { "id": "brace-storm", "description": "alternating braces with no structure", "repeat": { "unit": "{}", "count": 5000 }, "values": "card" },
    { "id": "long-path-segment", "description": "one segment of ten thousand characters", "prefix": "{card.", "repeat": { "unit": "z", "count": 10000 }, "suffix": "}", "values": "card" },
    { "id": "markdown-in-template", "description": "prose that is also active Markdown", "template": "see [here](javascript:alert(1)) for {card.number}", "values": "card" },
    { "id": "json-in-template", "description": "prose that is also a JSON fragment", "template": "\", \"leaked\": \"{card.number}", "values": "card" },
    { "id": "getter-path", "description": "a JavaBean getter reached by property name", "template": "{user.name}", "values": "user" },
    { "id": "getter-redacted", "description": "a JavaBean getter whose name the deny-list knows", "template": "{user.secret}", "values": "user", "expect": "redacted" },
    { "id": "redacted-bare-key", "description": "a bare placeholder whose key is the parameter name a deny-list would catch - the production every other redacted case here misses, because they all name a property path", "template": "login {password}", "values": "password-scalar", "expect": "redacted" },
    { "id": "credential-shaped-scalar", "description": "a JWT as a scalar placeholder value under a key no deny-list knows: redacted as a captured parameter, printed in full through a template until 2026-09-04", "template": "issued {value}", "values": "jwt-scalar", "expect": "redacted" },
    { "id": "control-char-scalar", "description": "a scalar placeholder value with a raw newline, which the property path would have folded and the scalar shortcut did not", "template": "note: {comment}", "values": "newline-scalar" },
    { "id": "whole-object-placeholder", "description": "names the whole object directly, with its redacted member reachable inside the ordinary field and depth caps - the control case: nothing here defeats the safe rendering, so the marker must show", "template": "audit {card}", "values": "card", "expect": "redacted" },
    { "id": "whole-object-redacted-past-the-field-cap", "description": "names the whole object, whose redacted component sits past the renderer's field cap - the safe rendering truncates the component away, so it carries no marker and a toString() fallback prints the secret", "template": "audit {wide}", "values": "wide" },
    { "id": "whole-object-redacted-below-the-depth-cap", "description": "names the whole object, whose redacted leaf sits below the renderer's depth cap - the walk stops before reaching it, so the safe rendering carries no marker and a toString() fallback prints the secret", "template": "audit {chain}", "values": "chain" }
  ]
}
