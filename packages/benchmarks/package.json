{
  "name": "@narrativetrace/benchmarks",
  "version": "0.1.0",
  "description": "Vitest benchmark suite for core, proxy, renderer, and trace-tree hot paths",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "vitest bench"
  },
  "devDependencies": {
    "@narrativetrace/core": "workspace:*",
    "@narrativetrace/proxy": "workspace:*"
  }
}
